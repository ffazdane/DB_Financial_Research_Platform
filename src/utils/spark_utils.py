"""spark_utils.py
Phase 2 — Spark Utility Functions

Helpers:
- get_spark() — get or create SparkSession
- delta_merge(df, target_table, merge_keys) — generic MERGE upsert
- read_delta(table_name) — read Delta table as DataFrame
- write_delta(df, table_name, mode) — write DataFrame to Delta table
"""
# TODO: implement in Phase 2
