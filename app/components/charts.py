"""charts.py
Phase 8 — Reusable Plotly Chart Components

Components:
- candlestick_chart(df, ticker) — OHLCV candlestick with volume
- fdts_line_chart(df, ticker) — FDTS fast/slow lines with signal markers
- ticker_strength_bar(df) — horizontal bar chart of strength scores
- strategy_distribution_pie(df) — strategy mix donut chart
"""
# TODO: implement in Phase 8
